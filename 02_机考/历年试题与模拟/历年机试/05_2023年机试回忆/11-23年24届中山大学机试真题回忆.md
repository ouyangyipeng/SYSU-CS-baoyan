# 23年24届中山大学机试真题回忆
> - 前三题考察 C++多态、面向对象等为主，后七题就是算法题，涉及知识点有图论、搜索、贪心、字符串等。考察还是比较全面的，采用 IOI 赛制，即时提交可见成绩，有部分分
> - 考核方式：机考（仅做参考） + 面试（100 分）
> - 机考一共有十题，每题100分且按样例给分（编译通过也有10分）。题目的内容包括了数据结构、算法、C++面向对象方面的知识（继承、多态、虚函数等等）、使用的是学院自己的oj平台，在正式机考前会有一个模拟测试，可以借模拟的机会提前熟悉一下正式的考试环境。C++方面的题目有3题左右，提前准备一下还是比较好拿分的，机考的平均得分大概是300-400分左右的样子，如果考得比较高的话，那面试可能会相对轻松，如果机考发挥得不好，也不要灰心，面试好好表现的话影响也不会有什么问题的！
> - 中山的机试编译成功就有分，而且还可以看到固定的测试用例，可以骗分，当时忘记骗了，随机测试用例不可见；中大机试很注重面向对象编程、输入输出、手写数据结构

### 部分回忆真题

 

### A1 输入输出

 
- 要求：实现程序，输入一个值，输出最小值和总和
 
- 输入示例：

```text
5
3 7 1 9 8
```

- 输出：

```text
1
28
```

这题重点在于适配给定接口，主程序已经提供，大致如下：

```cpp
#include <A1.hpp>


using namespace std;
int main(){


    ............
    cout<<arraycount(arr,n,min)<<endl;
    cout<<arraycount(arr,n,max)<<endl;//min和max在这个函数里都没有被定义！！！你需要在另一个文件用宏来搞min和max，但是我忘记语法了！！
    //貌似还有传入指针的问题
}
```

你需要从头实现A1.hpp，不可更改主程序。

 

### A2 数组的深拷贝

 

也是要自己写从头实现！然后A2.hpp里只能用指针，不可以用数组。

 
- 输入示例：

```text
4//数组长度
1 3 3 5//读入数组 
3//删除数字 
```

- 输出：

```text
4//数组长度 
1 3 3 5//数组print 
2//删除后长度 
1 5//删除后print 
```

锁住的主程序:

```cpp
#include <A2.hpp>


using namespace std;
int n;
int main(){


    ............
    cin>>n//这个是数组个数 
    num1();//忘记有没有显示调用无参构造了 
    num1.read(n);//自己实现read 
    num1.print(); 
    if(num1.size()>20){
        printf("Attention!");//要求你只能用指针分配地址空间，不可以用数组 
    }
    else{
    num2(num1);//#第二类构造方法，深拷贝num1 
    cin>>m; 
    num2.delete(m);//自己实现 
    num2.print(); 


    num2.print(); 
    }
}
```

### A3 继承关系的实现

 
- 要求：实现相应类，可以输出指定值
 
- 说明：主要考点是 C++ 继承、多态（virtual 方法 ）、多继承、虚拟继承（类和析构函数前面都要加 virtual ）、多继承的二义性（父类在继承祖父类的时候前面加 virtual ）、C++ 方法内读入字符串后处理并输出
 

### A4 连通性

 
- 要求：假设一个网格图，左上角坐标为 (0, 0) ，长 m 宽 n
 
- 输入描述：输入 m 和 n，代表 m 行 n 列，之后 m 行有 m-1 个数，代表从 (i,j) 到 (i,j+1) 的联通性，然后 n 行有 m 个数，表示 (i,j) 到 (i+1,j) 的连通性，给到的图中必存在一个环，请按字典序输出环的长度以及所在的坐标。
 
- 输入示例：

```text
3 3
10
01
11
100
011
```

- 输出示例：

```text
4
1 1
1 2
2 1
2 2
```

(0,0)

![image.png](%E5%9B%BE%E7%89%87/006-image.png.png)

### A5 二叉树

 
- 要求：读入 T 组，每组包含 N 对整数，分别代表第 n 号节点的左右节点编号，\-1 代表没有子节点，判断其是否是合法二叉树
 
- 输入示例

```text

```

- 输出示例：

```text

```

### A6 字符串反转

 
- 要求：读入 T 组，每组包含 N 个字符串，如果字符串 A 反转后与 B 相等，那么 A 与 B 匹配，确保每个字符串只会匹配一次，输出每组匹配字符串个数（范围忘了，字符串长度小于50还是100 ），没有就输出 \-1。
 
- 输入示例：

```text
3  
8  
cd  
dc  
eofgui  
iuasd  
daks  
wfej  
wa  
dsaui  
4  
absd  
owjdwjd  
dsba  
asoej  
weopaw  
2  
ee  
ehjr  
```

- 输出示例：

```text
2  
1  
-1  
```
